# 부동산 연구 회차 인계 — 2026-09-11

## 최종 목적

현재 가격 비교 모델을 토대로 잠재력 모델의 검증력을 높이고, 온라인 임장을 통해 매물 특성과 그 근거를 학습한다. 개인 주거 적합성은 일반 가격·잠재력 모델 학습과 분리한다. 회차 보고는 이 목적에 관련된 발견·미확인·다음 행동만 간단히 남긴다.

## 이번에 확인·준비한 것

- 현재 가격 비교는 운영 중이고, 잠재력은 연구 후보 단계다. 이번 회차에 잠재력 성능이나 실제 수익률이 개선됐다는 결과는 없다.
- 독립 시뮬레이션팀: 정우 실행, 민재 검수, 서진 모델 연결. 2023년 말→2024년, 2024년 말→2025년 및 예산별 보유/갈아타기 규약과 작은 엔진을 마련했다. 합성자료·저장 모의 검사 22개를 통과했다. 실제 역사 실험 0회.
- 기존 임장 DB 인계 25건의 구조를 검증했다: research 16, tasks 7, sources 1, checkpoints 1. 동일 내용 재사용·CAS 충돌 중단·PUT 응답 소실 후 재개를 구현했다. 실제 DB 조회·쓰기 0회. 기록은 모두 model_eligible=false를 유지한다.
- 이번 온라인 재확인은 두 후보의 광고 원문 접근 실패와 유효 공식 자료 부재로 새 사실 0건이다. 과거 관측 가격·타입을 현재 정보로 갱신하지 않았다. 상세 미확인은 기존 비공개 임장 기록에 이어서 관리한다.
- 공개 소스의 기존 API 키와 자동 키 탐색을 제거했다. 발급처 폐기·재발급은 별도 미완료다. PR에서 운영 데이터를 복원하던 오래된 Git 경로를 제거하고, 큰 후보 검증은 수동 저장 준비 검사 뒤에만 남겼다.
- 대표/팀 연구 보고 화면과 수집·학습 없는 UI 전용 배포 경로를 추가했다. 페이지의 날짜와 데이터 관측일을 구분하고 원래 데이터 해시를 보존한다. 실제 공개 배포는 GitHub Actions 성공 및 HTTP 검증 뒤에만 완료로 표시한다.

## 저장 조건 때문에 차단한 것

현재 세션에는 비공개 backend URL/token이 없다. 기존 `/ready`는 연결 상태만 알려주며 전체 무료 용량·사용량·보존·쓰기 복원 증거를 제공하지 않는다. 과거 연결 성공 로그만으로 이번 큰 실험의 안전성을 확정하지 않는다.

`python estate_research_storage.py`는 compact 결과와 종료코드 2로 차단한다. 이를 통과시키는 자가 작성 확인서나 `verified=true` 레코드는 없다. `LiveCapacityAdapter`는 미래 인터페이스이며 현재 운영 구현이 없다. 계측을 연결하기 전 실제자료 수집·큰 패널·학습·시뮬레이션을 실행하지 않는다. 기존 승인 일일/월간 증분 일정은 확장하지 않는다.

## 다음 실행 순서

1. 관리자 접근 환경에서 이전 키를 폐기·재발급하고 Actions secret을 등록한다. `main` 보호 정책도 적용한다. 현재 연결 도구에는 secret/branch-protection 수정이나 새 workflow dispatch 기능이 없었다.
2. [저장 준비 지침](storage-readiness.md)에 따라 실제 계정의 무료 구성·총 사용량·보존·복원 근거를 연결한다. 같은 자원을 쓰는 다른 프로젝트의 사용량도 합산한다. 작은 기록 이관과 대용량 실험의 조건을 혼동하지 않는다.
3. 기존 비공개 `apartment_round3_db_handoff_2026-09-11.json`을 찾아 [이관 CLI](../sync_estate_research_records.py)로 read-only 확인 후 충돌 없는 기록만 적용한다. 원본 의미 지문은 `a594ad9e2ec141c79e613c9ab73f52c6437b90debe14926e126df7dd0f16fa79`. 이는 원본 파일 바이트 SHA가 아닌 canonical JSON SHA다. 적용 결과의 서버 확인 버전·실패 키를 작은 비공개 체크포인트로 남긴다.
4. 두 단지의 네 미확인 질문에 대한 정확한 광고/공식 원문을 확보한다. 관측일·공개일·출처를 기록하고, 개인 적합성과 학습 가능 여부를 따로 판정한다. 2026년 임장 관측을 2023/24 입력에 소급하지 않는다.
5. [시뮬레이션 실행 인계](simulation/RUNBOOK.md)를 따라 당시 공개 패널과 비용 근거를 준비한다. 엄밀한 당시 원장이 없으면 신고지연 가정의 재구성 실험으로 명시한다. 저장 조건 통과 후 한 시점·한 예산·짧은 기간으로 먼저 실행한다.
6. 실제 세제·대출·임시거주 비용 모듈, 사후 최고 상승 상한·추가 대조군 집계, 모델 피처 연결은 후속 작업이다. 합성 결과를 투자 성과로 쓰지 않는다.
7. 검증 후 `Deploy dashboard to GitHub Pages`에서 `ui_only=true`, `refresh=false`로 화면을 공개하고 실제 manifest·research.html 해시를 확인한다. 즉시 실행 권한이 없으면 기존 예약 배포 이후 확인하며 배포 완료라고 미리 보고하지 않는다.

## 이어받기 규칙

실험에는 run ID, 입력·엔진·정책 해시, source snapshot, 최초 restore receipt, checkpoint version, 다음 날짜, 완료/미완료 범위, 차단 사유와 다음 명령을 남긴다. 충돌은 reread/recompute하며 새 receipt만 얻어 오래된 계산을 덮지 않는다. 원본·큰 중간결과는 압축 OCI, 인덱스·인계는 DB, Git에는 코드와 작은 공개 요약만 올린다.

작업 기준 원본은 main `e8acae5b40c55543128e5c0676e4450899193d9d`다. 작업 브랜치는 `codex/estate-research-ops-20260911`이며, 브랜치 이름만 보고 완료를 가정하지 말고 PR/Actions 상태를 확인한다. 충돌한 기존 PR #6은 누락 기능 대조 전 보존한다.
