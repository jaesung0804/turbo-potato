# 배포와 복구 — 2026-09-11 현재

현재 운영 저장소는 `RESEARCH_STORAGE=backend`의 Oracle·OCI입니다. Oracle은 검색·작업·인계 기록을, OCI는 압축 원본·모델·체크포인트의 불변 파일을 보존합니다. 코드 저장소에는 코드, 작은 공개 보고·명세만 추가합니다. 기존 Git 상태 브랜치는 이전 이력이며 backend 장애의 대체 저장소로 사용하지 않습니다.

## 화면·보고만 바꿀 때

`Deploy dashboard to GitHub Pages`를 수동 실행하고 기본값 `ui_only=true`, `refresh=false`를 유지합니다. `refresh_saved_site.py`는 기존 공개 manifest의 완전성 및 각 주요 데이터의 크기·SHA-256을 검사한 뒤, 최대 64MiB 범위에서 공개 데이터를 재사용하고 `web/` 화면만 교체합니다. 원본 DB 복원, 자료 수집, 모델 학습을 하지 않습니다.

자료의 `generated_at`, 수집일, 모델·데이터 해시는 바꾸지 않으며 `data_code_commit`에 자료를 만든 커밋을 유지합니다. `code_commit`은 새 UI 코드이고 `ui_source_release`는 직전 공개본의 manifest 해시입니다. 불완전·손상·한도 초과 데이터는 배포하지 않습니다. 실제 공개 URL의 모든 해시까지 확인한 뒤 완료로 보고합니다.

코드 push와 PR은 배포·수집·운영 데이터 복원을 유발하지 않습니다. PR에서는 작은 fixture 기반 unit/UI 검증을 실행합니다. 전체 후보/ML 검증은 `Validate complete estate dashboard`의 `full_candidate=true` 수동 실행으로 남겨두고, 먼저 저장 준비 상태 검사를 통과해야 합니다. 후보 검증은 상태를 publish하지 않습니다.

## 승인되어 있던 정기 데이터 갱신

기존 일일 Pages 일정은 UTC 20:35(한국시간 다음 날 05:35)이며 예약 실행은 지연될 수 있습니다. backend에서 원본·월 모델·잠재력 스냅샷을 복원하고 해시를 확인합니다. 최근 3개월과 과거 순환 1개월만 증분 갱신하며, 필요한 월 모델을 고정 정책으로 사용합니다. 기존 월간 잠재력 연구 일정도 유지합니다. 새로운 역사 자료 역수집이나 시뮬레이션은 이 일정에 추가하지 않습니다.

수동으로 전체 릴리스를 만들려면 `ui_only=false`를 명시합니다. `refresh=true`는 거래 수집도 요청합니다. 전체 릴리스는 KB 공개 지표 조회 및 해당 월 모델 생성이 필요할 수 있으므로 단순 UI 수정에는 사용하지 않습니다.

API 키는 GitHub Actions 비공개 secret `MOLIT_API_KEY`에서만 읽습니다. 공개 소스의 이전 키 탐색은 제거했습니다. secret이 없으면 일일 실행은 경고와 함께 기존 검증 자료·관측일을 보존하고, 명시적 수집 요청은 오류로 중단합니다. 별도 건축물 조회 도구는 비공개 `MOLIT_APT_BASIS_KEY` 또는 실행 시 인자를 요구합니다.

## 큰 실험과 복구

1. [저장 준비 검사](docs/storage-readiness.md)로 접속, 무료 한도, 잔여 용량, 압축 예산, 실제 복원 근거를 확인합니다. 현재 API에는 필요한 운영 용량 증거가 모두 없어 대용량 실행은 차단됩니다.
2. 최초 복원 receipt와 입력 snapshot ID·해시를 고정합니다. 큰 원본은 OCI 압축 파일, 인덱스·인계·진행 상태는 DB의 작은 레코드에 저장합니다.
3. 작업은 한 시점·한 예산·짧은 기간으로 먼저 수행하고 체크포인트에서 재개합니다. 개인 주거 조건은 공개 데이터에 포함하지 않습니다.
4. 상태 publish는 최초 receipt로 compare-and-swap을 수행합니다. 충돌하면 재조회·재계산하며, 오래된 결과에 새 receipt만 붙이지 않습니다. SQL enqueue 실패도 별도로 남깁니다.
5. 손상·불완전한 스냅샷을 빈 DB·Git 재업로드·무단 전체 재수집으로 대체하지 않습니다. 기존 정상 불변 스냅샷을 검증해 복구합니다.

## 남은 관리자 조치

- 공개 이력에 있었던 이전 API 키는 소스 삭제만으로 폐기되지 않습니다. 발급처에서 폐기·재발급 후 Actions secret을 등록해야 합니다. 발급처와 GitHub secret 설정에 접근 가능한 담당자가 처리합니다. 키를 보고서·프롬프트에 붙여넣지 않습니다.
- `main`은 점검 당시 보호되지 않았습니다. 관리자 설정에서 PR 필수, 최신 검증 성공, 강제 push·삭제 제한을 적용하고 팀 규모에 맞는 리뷰 정책을 정합니다. 브랜치 설정 API 권한이 없는 세션에서는 완료로 표시하지 않습니다.
- 기존 PR #6은 충돌한 수집 작업입니다. 누락 기능을 대조하기 전 자동 병합하거나 삭제하지 않습니다. 현재 DB 경로로 필요한 변경만 옮긴 뒤 정리합니다.
